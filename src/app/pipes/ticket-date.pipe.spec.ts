import { TicketDateHTMLPipe } from './ticket-date.pipe';

describe('TicketDatePipe', () => {
  it('create an instance', () => {
    const pipe = new TicketDateHTMLPipe();
    expect(pipe).toBeTruthy();
  });
});
