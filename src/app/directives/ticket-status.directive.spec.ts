import { TicketStatusDirective } from './ticket-status.directive';

describe('TicketStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new TicketStatusDirective();
    expect(directive).toBeTruthy();
  });
});
